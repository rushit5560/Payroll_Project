import 'package:flutter/material.dart';

class ForgotPasswordWidgetScreen extends StatelessWidget {
  const ForgotPasswordWidgetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}